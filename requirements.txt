# RecentInstallTracker - Python dependencies
# ============================================
# Install with:  pip install -r requirements.txt

# ── Required ─────────────────────────────────
# (all stdlib on Python 3.10+, nothing mandatory)

# ── Recommended: rich console output ─────────
rich>=13.0.0

# ── Optional: Windows Event Log scanning ─────
# Enables EventLogScanner (MSI installer events)
# Install:  pip install pywin32
# pywin32>=306
